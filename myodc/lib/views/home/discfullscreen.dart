import 'package:flutter/material.dart';

class DiscFullScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Disc Full Screen'),
      ),
      body: Center(
        child: Text('Disc Full Screen Content'),
      ),
    );
  }
}